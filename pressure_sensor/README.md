pressure_sensor
==========


Making it work
--------------


To build:

    cd pressure_sensor
    qmake
    make

To run (assuming you are logged into the RPi over ssh and no X-server is running):

    startx ./QwtExample
